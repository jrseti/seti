<!DOCTYPE html>
<html>
<head>
  <title>setiQuest Explorer</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

  <%if current_user%>

    <a href="/"><b>Home</b></a>
    Logged in as <%=current_user.name%>
    <%= link_to "Log Out", logout_path %>
    
    <%if current_user.role.to_sym == :admin%>
      | <b>Manage</b> <%= link_to "Users", users_path %> <%= link_to "Targets", targets_path %> 
      <%= link_to "Observations", observations_path %> 
      <%= link_to "Ranges", observation_ranges_path %> 
      <%= link_to "Assignments", assignments_path %>
    <%end%>
        
    <%= yield %>

  <%else%>
    Please log in via Facebook. We won't share your info.
    Log in via <%= link_to "Facebook", "/auth/facebook"%>
  <%end%>   
  

</body>
</html>
